{
  "name": "@switchboard/billing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo skip"
  },
  "dependencies": {
    "stripe": "^17.2.1",
    "@switchboard/shared": "workspace:*",
    "@switchboard/db": "workspace:*"
  },
  "devDependencies": {
    "@switchboard/tsconfig": "workspace:*",
    "typescript": "^5.6.3"
  },
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ]
}